private = {
  onPlayerStartEnter = function(vehicle, player, seat, jacked, door)
    return false
  end,

  onVehicleStartExit = function(vehicle, player, seat, jacked, door)
    return false
  end,

  checkAccess = function(player, vehicle)

  end,
}